# Protesis-Animales

> Proyecto generado desde `metodologia-root`. Metodología versión: ver `.claude/methodology.version`.

## Qué es
Describe en 3-8 líneas qué hace este proyecto y su alcance.

## Requisitos
- ...

## Instalación
```bash
<COMANDO>
```

## Ejecución
```bash
<COMANDO>
```

## Cómo está organizado
- `docs/STATUS.md` — estado global actual (empieza por aquí).
- `docs/MAP.md` — mapa de líneas de trabajo (topics) y su madurez.
- `docs/DECISIONS.md` — decisiones importantes (ADR).
- `docs/topics/<topic>/` — estado detallado de cada línea de trabajo.
- `.claude/` — reglas, agentes y comandos de la metodología.
- `.buzon/` — comunicación con otros proyectos.

## Cómo se trabaja aquí
Este proyecto sigue la metodología de `metodologia-root`. Lee `CLAUDE.md` antes de contribuir.
El arranque diario es **`/inicio`**. Resumen: una rama por topic, guardado con `/checkpoint`, cierre
reanudable con `/cierre-sesion`, y `/retomar <topic>` para seguir sin depender del chat anterior.
